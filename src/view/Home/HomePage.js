import React from "react";
import styles from "./homePage.module.css";

const HomePage = () => (
  <div className={styles.container}>
    <h1 className={styles.title}>Hello, this is main page</h1>
  </div>
);

export default HomePage;
